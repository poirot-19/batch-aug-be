spring.jpa.hibernate.ddl-auto = update
# Create the schema and destroy previous data.
# Create and then destroy the schema at the end of the session.
# update - Update the schema if necessary.
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/bookmyshow
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql = true
# whenever any query is getting fired by ORM, it will be logged to
# terminal as well.